import * as S from './Spinner.styled';

const Spinner = () => {
  return (
    <S.Container>
      <S.WrapperSpinner>
        <S.Spinner />
      </S.WrapperSpinner>
    </S.Container>
  );
};

export default Spinner;
